#!/usr/bin/env python3

from setuptools import setup

setup(
    name="radicale_homeassistant_password_auth",
    version="1.0.1",
    packages=["radicale_homeassistant_password_auth"],
    install_requires=["radicale>=3.0.0"])
